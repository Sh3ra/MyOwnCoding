#include <iostream>
    #include <stdio.h>
    #include <vector>
    #include <limits.h>
    #include <algorithm>
    #include <stack>
    #include <queue>
    #include <map>
    #include <cstring>
    #include <math.h>
    #define ll long long
using namespace std;

int main(int argc, char const *argv[])
{
    ll a,b;
    while (cin>>a>>b)
    {
        cout<<abs(a-b)<<endl;
    }
    
    return 0;
}
