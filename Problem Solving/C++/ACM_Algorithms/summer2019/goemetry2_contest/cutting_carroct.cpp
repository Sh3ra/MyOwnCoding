#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[])
{
    int n,h;
    cin>>n>>h;
    long double total_area,each_area;
    total_area=0.5*1*h;
    each_area=total_area/n;
    stack<long double>heights;
    long double base=1;
    for(int i=0;i<n-1;i++)
    {
        
    }
    return 0;
}
